<div class="content-body">

            <!-- Page Headings Start -->
            <div class="row justify-content-between align-items-center mb-10">

                <!-- Page Heading Start -->
                <div class="col-12 col-lg-auto mb-20">
                    <div class="page-heading">
                        <h3>کاربران</h3>
                    </div>
                </div><!-- Page Heading End -->

            </div><!-- Page Headings End -->

            <div class="row">

                <!--Manage Product List Start-->
                <div class="col-12">
                    <!-- Page Button Group Start -->
                <div class="col-12 col-lg-auto mb-20">
                    <div class="buttons-group">
                        <a href="/admin/users/create"><button class="button button-outline button-primary"> ایجاد کاربر جدید</button></a>
                    </div>
                </div><!-- Page Button Group End -->

                    <div class="table-responsive">
                        <table class="table table-vertical-middle">
                            <thead>
                                <tr>
                                    <th> نام</th>
                                    <th> ایمیل</th>
                                    <th> تاریخ انتشار</th>
                                    <th> پست</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each user}}          
                                <tr>
                                    <td>{{this.ful_name}}</td>
                                    <td>{{this.email}}</td>
                                    <td>{{this.created_at_persion}}</td>
                                    <td>{{this.role}}</td>
                                    <td>
                                        <div class="table-action-buttons">
                                            <a class="edit button button-box button-xs button-info" href="/admin/users/edit/{{this.id}}"><i class="zmdi zmdi-edit"></i></a>
                                            <a class="delete button button-box button-xs button-danger" href="/admin/users/delet/{{this.id}}"><i class="zmdi zmdi-delete"></i></a>
                                        </div>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>
                <!--Manage Product List End-->

            </div>

        </div>